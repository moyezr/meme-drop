{
  "name": "@memedrop/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run --package memedrop-api uvicorn memedrop_api.main:app --app-dir src --host 0.0.0.0 --port 3001 --reload",
    "build": "uv build --package memedrop-api --out-dir dist",
    "test": "uv run --package memedrop-api pytest tests",
    "test:integration": "uv run --package memedrop-api pytest -m integration tests",
    "typecheck": "uv run --package memedrop-api mypy src tests",
    "lint": "uv run --package memedrop-api ruff check .",
    "format": "uv run --package memedrop-api ruff format ."
  }
}
